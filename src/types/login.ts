export type TRole = "Keeper" | "Animal shelter";

export type TProfileFormValues = {
  avatar?: File;
  firstName?: string;
  lastName?: string;
  name?: string;
  city?: string;
  address?: string;
};
